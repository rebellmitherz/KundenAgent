# Engine & Closer — eigenständige Projekte, nicht hier verwalten
b2bbot/
ClouseAgent/

# Lizenzgenerator — NUR beim Verkäufer, nie im Kundenpaket
product/licensing/keygen.py

# Secrets — niemals einchecken
product/product_config.json
product/product_smtp.json
product_config.json
product_smtp.json
# auch Varianten wie product_smtp_kontakt.json (enthalten Mail-Passwort)
product/product_smtp*.json
product_smtp*.json

# Sperrliste / Kontaktdaten (echte E-Mail-Adressen = PII) — nie einchecken
product/config/

# Runtime-Output (Leads/Reports/Mail-Vorbereitung, enthält PII) — nie einchecken
/output/
product/output/

# Backups / Vorschau-Artefakte (Wegwerf)
*.bak_*
_final_preview_*.png

# Angebot-Profile (Mailtexte/PDF-Pfade pro Deployment) — kein Secret, aber lokale Config
product/product_profiles.json
*.env
.env

# Lock-Dateien
product/telegram/operator.lock

# Mandanten-Register (enthält Kunden-Secrets: Lizenz, Keys, Tokens) — nie einchecken
mandanten.json
product/data/platform/
product/data/mandanten/

# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
*.egg-info/
dist/
build/
.venv/
venv/
env/

# Laufzeit-Daten (Aufträge, Logs, Leads, Agent-Läufe)
product/data/orders/
product/data/logs/
product/data/output/
product/data/agent/

# Mandanten-Datenverzeichnisse (pro Kunde: product/data/<mandant_id>/) — nie einchecken
product/data/*/

# IDE / Agent-Werkzeuge
.claude/
.vscode/
.idea/
*.suo
*.user

# Windows
Thumbs.db
Desktop.ini
